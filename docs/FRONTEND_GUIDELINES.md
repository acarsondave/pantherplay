# Frontend Guidelines

- Pure TypeScript (No frameworks like React/Vue).
- Component state managed locally or via global Store.
- CSS is written in Vanilla CSS using CSS Variables for theming.
- **No generic elements**: Every button, input, and container must adhere to the premium design system.
- Smooth hash-based routing.
